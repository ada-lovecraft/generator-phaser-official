language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-phaser' ]; then cd .. && eval "mv $currentfolder generator-phaser" && cd generator-phaser; fi

